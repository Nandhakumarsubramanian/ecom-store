# Copy to .env and fill in values for live ERPNext.
# If ERPNEXT_URL is unset, the checkout endpoint returns a stub order so the demo still works.
PORT=3000
ERPNEXT_URL=
ERPNEXT_API_KEY=
ERPNEXT_API_SECRET=
ERPNEXT_DEFAULT_CUSTOMER=Guest Customer
